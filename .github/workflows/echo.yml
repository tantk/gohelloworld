name: Simple Startup

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
    
    
jobs:
  my-job:
    name: My Job
    runs-on: ubuntu-latest
    steps:
    - name: Print a greeting
      run: |
        echo Hello there!
        echo $(git log -1 --pretty=%B)
